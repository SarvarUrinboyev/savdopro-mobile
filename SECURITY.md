# Security policy

Do not report suspected vulnerabilities or exposed credentials in a public
issue. Use the private contact options on the repository owner's GitHub
profile.

The public repository must contain no production tokens, customer data,
private backend addresses, signing keys, or store credentials. Screenshots and
fixtures must use synthetic data.
